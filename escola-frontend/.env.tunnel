# Configuração para usar via Cloudflare Tunnel
# Substitua pela URL real do seu backend tunnel

# URL do backend via tunnel (SUBSTITUA pela URL real!)
VITE_API_URL=https://poll-identifier-taking-austria.trycloudflare.com

# Outras configurações mantidas
VITE_PORT=3001